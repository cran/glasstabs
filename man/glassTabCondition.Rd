% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glass_tabs.R
\name{glassTabCondition}
\alias{glassTabCondition}
\title{Build a conditionalPanel condition for a glasstabs widget}
\usage{
glassTabCondition(id, value)
}
\arguments{
\item{id}{The \code{id} passed to \code{\link[=glassTabsUI]{glassTabsUI()}}. \strong{Inside a Shiny module}
use \code{ns("tabs")} here (same id you passed to \code{glassTabsUI()}), NOT the
bare id you pass to \code{\link[=glassTabsServer]{glassTabsServer()}}.}

\item{value}{The tab value string (the \code{value} argument of the target
\code{\link[=glassTabPanel]{glassTabPanel()}}) that should trigger the condition.}
}
\value{
A single character string for use in \code{\link[shiny:conditionalPanel]{shiny::conditionalPanel()}}.
}
\description{
Returns a JavaScript condition string that evaluates to \code{TRUE} when the
specified glasstabs widget has a given active tab. Pass the result directly
to the \code{condition} argument of \code{\link[shiny:conditionalPanel]{shiny::conditionalPanel()}}.
}
\examples{
# Returns a plain JS condition string — no Shiny session needed:
glassTabCondition("main", "details")

# Inside a module — use ns() for the id:
# UI:   glassTabCondition(ns("tabs"), "details")
# This produces: "input['mymod-tabs-active_tab'] === 'details'"

# Full app example showing conditionalPanel usage:
if (interactive()) {
  library(shiny)
  ui <- fluidPage(
    useGlassTabs(),
    glassTabsUI(
      "main",
      glassTabPanel("overview", "Overview", selected = TRUE,
        p("Always visible.")),
      glassTabPanel("details", "Details",
        p("Detail pane."))
    ),
    conditionalPanel(
      condition = glassTabCondition("main", "details"),
      wellPanel("This panel only shows on the Details tab.")
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui, server)
}

}

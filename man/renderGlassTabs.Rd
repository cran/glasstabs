% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glass_tabs.R
\name{renderGlassTabs}
\alias{renderGlassTabs}
\title{Render a reactive glass tab UI}
\usage{
renderGlassTabs(expr, env = parent.frame(), quoted = FALSE)
}
\arguments{
\item{expr}{An expression that returns a \code{\link[=glassTabsUI]{glassTabsUI()}} tag object.}

\item{env}{The environment in which to evaluate \code{expr}.}

\item{quoted}{Logical. Whether \code{expr} is already quoted.}
}
\value{
A render function suitable for assigning to an \code{output} slot.
}
\description{
Server-side render function that pairs with \code{\link[=glassTabsOutput]{glassTabsOutput()}}. The
expression should return a \code{\link[=glassTabsUI]{glassTabsUI()}} call. After each render the
glasstabs JavaScript engine is automatically reinitialised so animations and
event handlers are correctly attached to the new DOM nodes.
}
\examples{
if (interactive()) {
  library(shiny)

  ui <- fluidPage(
    useGlassTabs(),
    radioButtons("theme", "Theme", c("dark", "light"), inline = TRUE),
    glassTabsOutput("tabs_out")
  )

  server <- function(input, output, session) {
    output$tabs_out <- renderGlassTabs({
      glassTabsUI(
        "themed",
        glassTabPanel("x", "X", selected = TRUE, p("X content")),
        glassTabPanel("y", "Y", p("Y content")),
        theme = input$theme
      )
    })
  }

  shinyApp(ui, server)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glass_multiselect.R
\name{updateGlassMultiSelect}
\alias{updateGlassMultiSelect}
\title{Update a glassMultiSelect widget}
\usage{
updateGlassMultiSelect(
  session,
  inputId,
  choices = NULL,
  selected = NULL,
  check_style = NULL,
  shape = NULL,
  disabled = NULL,
  disabled_choices = NULL
)
}
\arguments{
\item{session}{Shiny session.}

\item{inputId}{Input id of the widget.}

\item{choices}{New choices, or \code{NULL} to keep current choices.}

\item{selected}{New selected values, or \code{NULL} to keep current
selection. Use \code{character(0)} to clear.}

\item{check_style}{Optional new style string. One of \code{"checkbox"},
\code{"check-only"}, or \code{"filled"}. Defaults to \code{NULL}, which
keeps the current style unchanged.}

\item{shape}{Optional new corner style. One of \code{"rounded"} or
\code{"square"}. Defaults to \code{NULL}, which keeps the current shape
unchanged.}

\item{disabled}{Optional logical. \code{TRUE}/\code{FALSE} toggles the
whole-widget disabled state. Defaults to \code{NULL}, which leaves it
unchanged.}

\item{disabled_choices}{Optional character vector of choice values to render
as disabled. Defaults to \code{NULL}, which leaves disabled choices
unchanged.}
}
\value{
No return value. Called for its side effect of updating the
client-side widget.
}
\description{
Update the available choices and/or current selection of an existing
\code{\link[=glassMultiSelect]{glassMultiSelect()}} input.
}
\details{
This function now follows Shiny-style update semantics more closely:

\itemize{
\item \code{choices = NULL} leaves choices unchanged
\item \code{selected = NULL} leaves selection unchanged
\item \code{selected = character(0)} clears the selection
}

When \code{choices} is supplied and \code{selected} is not, the browser side
keeps the intersection of the current selection and the new set of choices.
}
\seealso{
Other glass select widgets: 
\code{\link{glassMultiSelect}()},
\code{\link{glassMultiSelectServer}()},
\code{\link{glassMultiSelectValue}()},
\code{\link{glassSelect}()},
\code{\link{glassSelectServer}()},
\code{\link{glassSelectValue}()},
\code{\link{updateGlassSelect}()}
}
\concept{glass select widgets}

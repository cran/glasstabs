% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glassSelect.R
\name{glassSelect}
\alias{glassSelect}
\title{Animated glass single-select dropdown}
\usage{
glassSelect(
  inputId,
  choices,
  selected = NULL,
  label = NULL,
  placeholder = "Select an option",
  searchable = TRUE,
  clearable = FALSE,
  include_all = FALSE,
  all_choice_label = "All categories",
  all_choice_value = "__all__",
  check_style = c("checkbox", "check-only", "filled"),
  theme = "dark",
  shape = c("rounded", "square"),
  width = NULL,
  disabled = FALSE,
  disabled_choices = NULL,
  server = FALSE,
  server_limit = 50L,
  server_min_chars = 0L
)
}
\arguments{
\item{inputId}{Shiny input id.}

\item{choices}{Named or unnamed character vector of choices.}

\item{selected}{Initially selected value. Defaults to \code{NULL}.}

\item{label}{Optional field label shown above the widget.}

\item{placeholder}{Trigger label when nothing is selected.}

\item{searchable}{Logical. Show search input inside dropdown? Default
\code{TRUE}.}

\item{clearable}{Logical. Show clear control for removing the current
selection? Default \code{FALSE}.}

\item{include_all}{Logical. Prepend an explicit "All" option. Default
\code{FALSE}.}

\item{all_choice_label}{Label used for the explicit "All" option.}

\item{all_choice_value}{Value used for the explicit "All" option.}

\item{check_style}{One of \code{"checkbox"} (default),
\code{"check-only"}, or \code{"filled"}.}

\item{theme}{Color theme. One of \code{"dark"} (default) or \code{"light"},
or a \code{\link[=glass_select_theme]{glass_select_theme()}} object.}

\item{shape}{Corner style for the trigger and dropdown. One of
\code{"rounded"} (default) for the signature glass look, or
\code{"square"} for crisp, selectize-style corners so the widget sits
neatly alongside native Shiny \code{selectizeInput()} controls.}

\item{width}{Optional widget width passed to
\code{shiny::validateCssUnit()}, e.g. \code{100\%} or \code{240px}. When
\code{NULL} (default) the trigger keeps its intrinsic width.}

\item{disabled}{Logical. When \code{TRUE} the whole widget is greyed out and
non-interactive. Default \code{FALSE}.}

\item{disabled_choices}{Optional character vector of choice values to render
as disabled (non-selectable) rows. Default \code{NULL}.}

\item{server}{Logical. If \code{TRUE}, render only an initial slice of
choices and use \code{\link[=glassSelectServer]{glassSelectServer()}} to search the full choice set from
the Shiny server. Default \code{FALSE}.}

\item{server_limit}{Maximum number of choices rendered initially and returned
for each server-side search. Default \code{50}.}

\item{server_min_chars}{Minimum search characters required before server-side
matching filters choices. Default \code{0}.}
}
\value{
An \code{htmltools::tagList} containing the single-select trigger,
dropdown panel, and scoped \code{<style>} block.
}
\description{
A stylized single-select Shiny input with optional search, clear control,
selection-marker styling, and programmatic updates via \code{\link[=updateGlassSelect]{updateGlassSelect()}}.
}
\details{
The widget registers one Shiny input:

\itemize{
\item \code{input$<inputId>} : selected value as a length-1 character string,
or \code{NULL} when nothing is selected
}
}
\examples{
fruits <- c(Apple = "apple", Banana = "banana", Cherry = "cherry")

fruit_select <- glassSelect("fruit", fruits)

selected_fruit <- glassSelect(
  "fruit",
  fruits,
  selected = "banana",
  clearable = TRUE
)

all_fruits <- glassSelect(
  "fruit",
  fruits,
  include_all = TRUE,
  all_choice_label = "All fruits",
  all_choice_value = "__all__"
)

filled_fruit <- glassSelect(
  "fruit",
  fruits,
  check_style = "filled"
)

}

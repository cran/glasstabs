% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dependencies.R
\name{useGlassTabs}
\alias{useGlassTabs}
\title{Attach glasstabs CSS and JS dependencies}
\usage{
useGlassTabs()
}
\value{
An \code{htmltools::htmlDependency} object (invisible to the user,
consumed by Shiny's renderer).
}
\description{
Call this once in your UI — either inside \code{fluidPage()}, \code{bs4DashPage()},
or any other Shiny page wrapper. It injects the required CSS and JS as
proper \code{htmltools} dependencies so they are deduplicated automatically.
}
\examples{
if (interactive()) {
  library(shiny)
  ui <- fluidPage(
    useGlassTabs(),
    glassTabsUI("demo",
      glassTabPanel("A", "Tab A", p("Content A")),
      glassTabPanel("B", "Tab B", p("Content B"))
    )
  )
  server <- function(input, output, session) {}
  shinyApp(ui, server)
}

}

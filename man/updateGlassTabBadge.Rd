% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glass_tabs.R
\name{updateGlassTabBadge}
\alias{updateGlassTabBadge}
\title{Update the badge count on a glass tab}
\usage{
updateGlassTabBadge(session, id, value, count)
}
\arguments{
\item{session}{Shiny session object.}

\item{id}{Module id matching the \code{id} passed to \code{\link[=glassTabsUI]{glassTabsUI()}}.}

\item{value}{Value of the tab to update.}

\item{count}{Integer count to display. Values above 99 are shown as
\code{"99+"}. \code{0} or \code{NA} hides the badge.}
}
\value{
Called for its side effect; returns \code{NULL} invisibly.
}
\description{
Adds or updates a small numeric badge on a tab button — useful for
surfacing counts such as unread items, pending rows, or notification
totals. Set \code{count} to \code{0} or \code{NA} to hide the badge.
}
\examples{
if (interactive()) {
  library(shiny)
  ui <- fluidPage(
    useGlassTabs(),
    glassTabsUI(
      "tabs",
      glassTabPanel("inbox",  "Inbox",  p("Messages here"), selected = TRUE),
      glassTabPanel("sent",   "Sent",   p("Sent items")),
      glassTabPanel("drafts", "Drafts", p("Draft items"))
    ),
    actionButton("refresh", "Refresh counts")
  )
  server <- function(input, output, session) {
    observeEvent(input$refresh, {
      updateGlassTabBadge(session, "tabs", "inbox",  count = sample(1:20, 1))
      updateGlassTabBadge(session, "tabs", "drafts", count = sample(0:5, 1))
    })
  }
  shinyApp(ui, server)
}
}

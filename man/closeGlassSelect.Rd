% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/close_selects.R
\name{closeGlassSelect}
\alias{closeGlassSelect}
\alias{closeGlassMultiSelect}
\alias{closeAllGlassSelects}
\title{Close open glass select dropdowns}
\usage{
closeGlassSelect(session = shiny::getDefaultReactiveDomain(), inputId)

closeGlassMultiSelect(session = shiny::getDefaultReactiveDomain(), inputId)

closeAllGlassSelects(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{session}{Shiny session. Defaults to the current reactive domain.}

\item{inputId}{Input id of the widget.}
}
\value{
No return value. Called for the side effect of closing dropdowns in
the browser.
}
\description{
Programmatically closes one \code{glassSelect()} dropdown, one
\code{glassMultiSelect()} dropdown, or every open glasstabs select dropdown in
the current Shiny session.
}
\details{
These helpers are useful before switching tabs, opening modals, rebuilding
UI, or changing layouts that can otherwise leave a dropdown visually open
after its trigger moved or disappeared.
}
\examples{
if (interactive()) {
  library(shiny)

  ui <- fluidPage(
    useGlassTabs(),
    actionButton("close", "Close dropdowns"),
    glassSelect("region", c(North = "north", South = "south")),
    glassMultiSelect("filters", c(A = "a", B = "b"))
  )

  server <- function(input, output, session) {
    observeEvent(input$close, {
      closeAllGlassSelects(session)
    })
  }

  shinyApp(ui, server)
}

}

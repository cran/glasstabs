% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glass_tabs.R
\name{disableGlassTab}
\alias{disableGlassTab}
\alias{enableGlassTab}
\title{Disable or enable a glass tab}
\usage{
disableGlassTab(session, id, value)

enableGlassTab(session, id, value)
}
\arguments{
\item{session}{Shiny session object.}

\item{id}{Module id matching the \code{id} passed to \code{\link[=glassTabsUI]{glassTabsUI()}}.}

\item{value}{Value of the tab to disable or enable.}
}
\value{
Called for its side effect; returns \code{NULL} invisibly.
}
\description{
\code{disableGlassTab()} grays out a tab and prevents the user from clicking it
without removing it from the navigation bar. \code{enableGlassTab()} reverses
this. Unlike \code{\link[=hideGlassTab]{hideGlassTab()}}, a disabled tab remains visible.
}
\examples{
if (interactive()) {
  library(shiny)
  ui <- fluidPage(
    useGlassTabs(),
    glassTabsUI(
      "tabs",
      glassTabPanel("a", "A", p("Tab A"), selected = TRUE),
      glassTabPanel("b", "B", p("Tab B")),
      glassTabPanel("locked", "Locked", p("Locked content"))
    ),
    checkboxInput("unlocked", "Unlock tab", FALSE)
  )
  server <- function(input, output, session) {
    # Start with "locked" tab disabled
    observe({
      if (input$unlocked) enableGlassTab(session, "tabs", "locked")
      else                disableGlassTab(session, "tabs", "locked")
    })
  }
  shinyApp(ui, server)
}
}
